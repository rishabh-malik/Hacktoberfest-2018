# volSphere calculates the volume of a sphere
def volSphere():

    rad = float(input("What is the radius of the sphere? "))
    
    vol = (4 / 3) * pi * (r**3)
    
    print("Volume is " + vol)
    
    return vol
    
volSphere()    
